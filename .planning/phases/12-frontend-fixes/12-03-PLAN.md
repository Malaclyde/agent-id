---
phase: 12-frontend-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/OverseerDashboard.tsx
  - frontend/src/components/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Delete Account button is visible but disabled with Coming Soon tooltip"
    - "Logout button exists in header and functions correctly"
  artifacts:
    - path: "frontend/src/pages/OverseerDashboard.tsx"
      provides: "User Info page with disabled delete account button"
      contains: "Delete Account"
    - path: "frontend/src/components/Header.tsx"
      provides: "Header with logout button"
      contains: "Logout"
  key_links:
    - from: "frontend/src/pages/OverseerDashboard.tsx"
      to: "UserInfo"
      via: "component function"
      pattern: "UserInfo.*button"
---

<objective>
Add disabled Delete Account button with Coming Soon tooltip to User Info page, verify logout functionality works.

Purpose: Per user decision - add disabled button with tooltip (feature coming in next milestone).
Output: Modified OverseerDashboard.tsx with delete button, verified Header.tsx logout
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-frontend-fixes/12-CONTEXT.md
@.planning/phases/12-frontend-fixes/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add disabled Delete Account button with tooltip to User Info page</name>
  <files>frontend/src/pages/OverseerDashboard.tsx</files>
  <action>
1. Find the UserInfo component (lines 369-397)
2. Add a disabled button with title tooltip after the existing form fields:
   ```tsx
   <div className="form-group" style={{ marginTop: '1.5rem' }}>
     <button 
       className="btn btn-danger" 
       disabled 
       title="Coming in next milestone"
     >
       Delete Account
     </button>
   </div>
   ```

This implements the locked decision: "ADD disabled button with Coming Soon tooltip on hover, Button should be visible but not clickable, Tooltip text: Coming in next milestone"
  </action>
  <verify>
Run `npm run build` in frontend directory to verify TypeScript compilation succeeds
  </verify>
  <done>
User Info page displays a disabled "Delete Account" button. Hovering over it shows tooltip "Coming in next milestone".
  </done>
</task>

<task type="auto">
  <name>Verify logout functionality exists and works correctly</name>
  <files>frontend/src/components/Header.tsx</files>
  <action>
1. Read Header.tsx to confirm logout button exists
2. Verify it calls the logout function from useAuth context
3. Check that AuthContext.tsx has proper logout implementation that calls POST /v1/overseers/logout
4. No changes needed - this is verification only per research finding: "Status: No changes needed - this already works!"

The logout functionality:
- Header.tsx lines 20-22: `<button className="btn btn-secondary" onClick={logout}>Logout</button>`
- AuthContext.tsx line 77: logoutOverseerAndClear() calls the API and clears local storage
- Backend: POST /v1/overseers/logout revokes session

This verifies the locked decision: "ADD logout option in UI header/menu, Already exists at POST /v1/overseers/logout"
  </action>
  <verify>
Manual verification: Login as overseer, click logout button, verify session is cleared and user is redirected to login
  </verify>
  <done>
Logout button exists in header and correctly calls the logout API endpoint to invalidate the session.
  </done>
</task>

</tasks>

<verification>
1. Navigate to User Info page (/overseer/dashboard/user-info)
2. Verify Delete Account button is visible but disabled
3. Hover over button - verify tooltip shows "Coming in next milestone"
4. Click button - verify nothing happens (disabled)
5. Verify logout button exists in header
6. Test logout flow - verify user is logged out
</verification>

<success_criteria>
- [ ] Delete Account button visible and disabled
- [ ] Tooltip shows "Coming in next milestone" on hover
- [ ] Button click does nothing (disabled)
- [ ] Logout button exists in header
- [ ] Logout properly invalidates session
- [ ] TypeScript builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-fixes/12-03-SUMMARY.md`
</output>
