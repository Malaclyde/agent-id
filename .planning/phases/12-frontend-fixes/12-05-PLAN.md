---
phase: 12-frontend-fixes
plan: 05
type: execute
wave: 3
depends_on:
  - "04"
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 7 frontend fixes are implemented and working"
    - "Subscription tier limits display correctly (BASIC: 10, PRO/PREMIUM: unlimited)"
  artifacts:
    - path: "docs/v1/"
      provides: "Documentation updates if needed"
  key_links:
    - from: "frontend"
      to: "backend"
      via: "API calls"
      pattern: "All endpoints wired"
---

<objective>
Verify all frontend fixes work correctly and update any documentation if needed.

Purpose: Final verification that all 7 fixes are complete and functional.
Output: Verified implementation with test summary
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-frontend-fixes/12-CONTEXT.md
@.planning/phases/12-frontend-fixes/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Verify tier limits display correctly</name>
  <files>backend/src/services/subscription-config.ts</files>
  <action>
1. Read subscription-config.ts to verify tier limits:
   - BASIC: max_clients = 10, max_oauth_per_period = -1 (unlimited)
   - PRO: max_clients = -1, max_oauth_per_period = -1
   - PREMIUM: max_clients = -1, max_oauth_per_period = -1

2. Verify frontend displays match:
   - RegisteredClients.tsx shows client count correctly
   - SubscriptionManagement.tsx shows tier limits in CurrentSubscriptionCard

3. No code changes needed - this is verification that FIX-FE-02 is already implemented correctly per research: "This already matches the requirements! The frontend displays are correct."

This verifies: "FIX subscription tier limits (BASIC: 10 clients, PRO/PREMIUM: unlimited)"
  </action>
  <verify>
Check subscription-config.ts has correct limits, verify frontend displays match
  </verify>
  <done>
Tier limits verified: BASIC=10 clients, PRO/PREMIUM=unlimited. Frontend displays match backend config.
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>All 7 frontend fixes implemented in plans 12-01 through 12-04</what-built>
  <how-to-verify>
Complete verification checklist:

1. FIX-FE-01 (Token picker removal):
   - [ ] Open OAuth Clients page
   - [ ] Click "Register New Client"
   - [ ] Verify NO dropdown for "Token Endpoint Auth Method"
   - [ ] Verify "Ed25519 Public Key" field IS visible
   - [ ] Verify "Generate Key" button IS visible

2. FIX-FE-02 (Tier limits):
   - [ ] Navigate to Subscription Management
   - [ ] Verify BASIC shows 10 client limit
   - [ ] Verify PRO/PREMIUM show unlimited

3. FIX-FE-03 (Logout):
   - [ ] Verify Logout button in header
   - [ ] Click Logout - verify session cleared

4. FIX-FE-04 (Delete button):
   - [ ] Navigate to User Info page
   - [ ] Verify "Delete Account" button visible but disabled
   - [ ] Hover - verify tooltip "Coming in next milestone"

5. FIX-FE-05 (Tier comparison):
   - [ ] Navigate to Subscription Management
   - [ ] Verify tier comparison table NOT displayed

6. FIX-FE-06 (Subscription cancel):
   - [ ] With BASIC/PRO/PREMIUM, verify Cancel Subscription button visible
   - [ ] Click Cancel - verify modal appears
   - [ ] Confirm cancellation - verify API called

7. FIX-FE-07 (Testing):
   - [ ] Run `npm run build` in frontend - no errors
   - [ ] Run `npm run build` in backend - no errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Build verification: npm run build in both frontend and backend passes
2. Manual verification: All 7 fixes checked via human-verify checkpoint
3. Check that no deferred items are implemented (delete account functionality)
</verification>

<success_criteria>
- [ ] FIX-FE-01: Token picker removed, public key always visible
- [ ] FIX-FE-02: Tier limits correct (BASIC 10, PRO/PREMIUM unlimited)
- [ ] FIX-FE-03: Logout works
- [ ] FIX-FE-04: Delete button disabled with tooltip
- [ ] FIX-FE-05: Tier comparison removed
- [ ] FIX-FE-06: Cancel subscription works
- [ ] FIX-FE-07: Builds pass, no deferred items
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-fixes/12-05-SUMMARY.md`
</output>
