---
phase: 25-frontend-update
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/ShadowClaim.tsx
  - frontend/src/index.css
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "ShadowClaim instructions use CSS classes from index.css (not inline styles)"
    - "All elements have border-radius: 0 (squared corners)"
    - "Colors use project's color palette (coral/amber theme)"
  artifacts:
    - path: "frontend/src/pages/ShadowClaim.tsx"
      provides: "Shadow claim UI with proper styling"
      contains: "CSS classes, border-radius: 0"
    - path: "frontend/src/index.css"
      provides: "Shared styles for shadow claim components"
---

<objective>
Replace inline styles in ShadowClaim.tsx with CSS classes from the project stylesheet.
</objective>

<context>
@.planning/phases/25-frontend-update/25-UAT.md

Gap: ShadowClaim.tsx uses inline `style={{}}` props instead of CSS classes. This includes using inline colors, border-radius, padding instead of the project's coral/amber theme with squared corners.
</context>

<tasks>

<task type="auto">
  <name>Replace inline styles with CSS classes in ShadowClaim.tsx</name>
  <files>frontend/src/pages/ShadowClaim.tsx, frontend/src/index.css</files>
  <action>
Replace all inline styles in ShadowClaim.tsx with CSS classes. Focus on the main content areas:

1. Loading state (lines 224-247):
   - Replace `style={{display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', minHeight: '60vh', padding: '1rem'}}` with existing `.loading` class or add `.shadow-claim-loading` class
   - Add corresponding CSS to index.css if needed

2. Error state container (lines 430-477):
   - Replace inline styles with `.card` class plus modifier classes
   - Use CSS variables for colors: `var(--primary)`, `var(--text)`, etc.

3. Expired state container (lines 480-547):
   - Replace inline styles with CSS classes

4. Main content card (lines 583-811):
   - Replace `style={{display: 'flex', alignItems: 'center', justifyContent: 'center', minHeight: '60vh', padding: '1rem'}}` with page container class
   - Replace card styles with `.card` class
   - Replace countdown timer background from `#fef3c7` to use CSS class with `var(--warning)` or create `.timer-box` class
   - Replace instruction block background from `#1f2937` to use CSS class with dark theme
   - Ensure ALL elements have `border-radius: 0` (user requirement - squared corners)
   - Use project colors: `--primary: #FF6F61`, `--warning: #D4A373`, `--text: #6B4226`, `--surface: #FFFFFF`, `--background: #FFF8F0`

5. Code block styling (URL and body display):
   - Replace `#1f2937` backgrounds with `.code-block` class
   - Replace `#111827` with the same

6. Button styling:
   - Replace inline button styles with `.btn` and `.btn-primary`/`.btn-secondary` classes
   - Add `border-radius: 0` where needed

7. Add missing CSS classes to index.css:
   - `.shadow-claim-container`: main container styling
   - `.shadow-claim-card`: card wrapper styling  
   - `.timer-box`: countdown timer box with warning colors
   - `.instructions-box`: dark background for instructions
   - `.code-box`: styled code blocks with proper colors
   - `.waiting-indicator`: waiting for agent message box

Key color replacements:
- `#3b82f6` (blue) → `var(--primary)` (#FF6C61 - coral)
- `#10b981` (green) → `var(--success)` (#6B8E23)
- `#ef4444` (red) → `var(--error)` (#BF544C)
- `#f59e0b` (amber) → `var(--warning)` (#D4A373)
- `#1f2937` (dark gray) → `#6B4226` (project text color)
- `#111827` (darker) → use project dark theme or adjust
- `#fef3c7` (light yellow) → `var(--surface-alt)` (#FFF5EB)
- `#eff6ff` (light blue) → `var(--surface-alt)` (#FFF5EB)
  </action>
  <verify>
- Run `npm run build` in frontend directory
- Verify all components render with correct styles
- Check that all corners are squared (border-radius: 0)
  </verify>
  <done>
- No inline style props in ShadowClaim.tsx main content
- All elements use CSS classes
- All corners squared (border-radius: 0)
- Colors match project palette
  </done>
</task>

</tasks>

<verification>
- Frontend builds without errors
- Visual inspection shows correct styling (squared corners, project colors)
- Test 3 passes: instructions display correctly with proper styling
</verification>

<success_criteria>
- Test 3 passes: Agent confirmation instructions styled correctly
- No inline styles used (except where absolutely necessary)
- All elements have border-radius: 0
- Color scheme matches project theme
</success_criteria>

<output>
After completion, create `.planning/phases/25-frontend-update/25-11-SUMMARY.md`
</output>
